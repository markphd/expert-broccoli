import {Layout} from '../components/Layout';
import styles from '../styles/Home.module.css';

export default function About() {
  return(
    <Layout propsHeader="About">
      <p>This is a paragraph for About page</p>
    </Layout>
  )
}